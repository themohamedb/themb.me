# InstantDB — server-side only. Never commit real values.

# Public app ID (safe for client when used correctly)
NEXT_PUBLIC_INSTANTDB_APP_ID=

# Admin token — SERVER ONLY. Never expose to client or commit real values.
INSTANTDB_ADMIN_TOKEN=
